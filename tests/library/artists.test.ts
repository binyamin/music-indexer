import assert from 'node:assert/strict';
import { describe, it } from 'node:test';

describe('buildLibrary()', () => {
	describe('artists', () => {
		it.todo('creates an artist per track artist');
		it.todo('creates an artist per album artist');
		it.todo('dedupes artists');
	});
});
